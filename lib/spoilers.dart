import 'package:flutter/widgets.dart';

import 'spoiler.dart';

class Spoilers extends StatelessWidget {
  final List<Spoiler> spoilers;

  const Spoilers({this.spoilers});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
